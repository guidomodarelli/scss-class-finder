# AGENTS

## Installation

- Always install the extension using `./install.sh`.
- Do not install the extension by manually packaging a `.vsix` and invoking `code --install-extension` with a hardcoded version.
- The installer script is the source of truth for resolving the extension version from `package.json`.
